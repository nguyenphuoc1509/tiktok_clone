spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tiktok?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: "12345"            # nên để trong dấu nháy
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update           # create | create-drop | update | validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

server:
  port: 8080

springdoc:
  packages-to-scan: com.phuocnt.tiktok.controller
  api-docs:
    enabled: true
  swagger-ui:
    path: /swagger-ui.html

app:
  jwt:
    secret: "Wo7So53gB0psFKuAga8QNNAbFX5o5nKSS0EXr0SqwT4KIbv3e5gBoIOW8zwSIoGL"
    expiration-seconds: 3600
